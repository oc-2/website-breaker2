[build]
  base = "geo-search/"
  publish = "dist/geo-search"
  command = "yarn run start"

[dev]
  command = "yarn run start"
  functions = "netlify/functions"

[[redirects]]
  from = "/geo-search/*"
  to = "/geo-search/:splat"
  status = 200
  force = true