[build]
  base = "geo-search/"
  publish = "dist/geo-search"
  command = "ng build"

[dev]
  command = "ng build"
  functions = "netlify/functions"

[[redirects]]
  from = "/geo-search/*"
  to = "/geo-search/:splat"
  status = 200
  force = true